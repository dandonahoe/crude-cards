import { Box } from '@mantine/core';
import { RFC } from '@app/ui/type';


export const GameSettings : RFC = () => {

    return (
        <Box>
            {'Game Settings'}
        </Box>
    );
}

